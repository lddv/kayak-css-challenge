In this directory you'll find a minimal HTML file, an empty CSS file, a logo PNG,
and a design PNG.
Please update the included page so that it looks like the design PNG. This is a
CSS exercise, so you should make as few changes to the markup as possible.
